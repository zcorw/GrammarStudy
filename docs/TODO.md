# TODO

## 说明

本文档记录当前项目相对于既定目标仍未完成的任务，并同步标注本轮已落地的进度。

当前项目目标：

- 日语文法学习 PWA
- 主链路：`推荐/搜索 -> 复用 -> 创建 -> 学习/练习 -> 入公共库沉淀`
- 首版目标：易维护、可上线、小规模正式使用

当前实现状态：

- 前端已切到真实 API，不再依赖内存仓储
- 后端已完成 PostgreSQL、Alembic、JWT、统一错误响应与基础鉴权
- 创建流程、收藏、最近浏览、练习记录、我的文法均已落到数据库
- 已补最小 API 烟测与统一检查脚本

## P0 当前主链路缺口

- [x] 前端从内存仓储切换到真实 API
- [x] 前端接入推荐、搜索、详情、练习、我的文法等真实接口
- [x] 创建流程从占位页改为真实多步骤状态流
- [x] 创建流程接口接入真实 PostgreSQL 持久化，而不是仅返回占位结果
- [x] 创建完成后的“入公共库”改为真实写库
- [x] 登录流程接入真实账号表，而不是仅返回审批状态占位响应
- [x] 邀请码 / 审核状态接入真实数据库字段和判断逻辑
- [x] 收藏、最近浏览、练习记录、我的文法改为真实用户数据存取

## P0 数据与后端

- [x] 为 `CreationSession` 设计并落地数据库模型与 Alembic 迁移
- [x] 为用户收藏、最近浏览、练习记录设计并落地数据库模型与迁移
- [ ] 为邀请码、邀请码使用记录、审核状态设计并落地数据库模型与迁移
说明：邀请码与审核状态已落地；独立的邀请码使用记录表仍未单独拆出。
- [x] 为自由造句点评结果设计并落地数据库模型与迁移
- [x] 补正式的 PostgreSQL repository，替代占位 usecase 返回值
- [ ] 为搜索增加更接近目标设计的匹配策略：标准化写法、读音、标签辅助排序
- [ ] 评估并接入 `pg_trgm` 及相关索引
- [ ] 将 demo seed 与正式 seed 区分开，避免生产环境误写测试数据

## P0 API 与契约

- [x] 补统一错误响应结构：`error.code / error.message / error.details`
- [x] 为所有受保护接口补统一鉴权依赖
- [x] 明确 JWT 生命周期、签发与校验逻辑
- [ ] 为游客 session 与登录态切换补状态迁移方案
- [x] 补 `/api/v1/grammar/recommended`、`/api/v1/user/my-grammar` 等接口的真实业务实现
- [x] 为创建流程接口补真实状态机与失败态
- [ ] 为 API 响应补分页、排序、空状态约定

## P0 前端实现

- [ ] 将 `ui-static/preview-site` 的交互细节完整映射进 React 实现
- [x] 搜索页支持真实 query 参数驱动与错误态展示
- [ ] 详情页补完整结构化内容：近义文法对比、常见错误、例句展示一致性
- [x] 练习页补真实答题提交与反馈逻辑
- [x] 自由造句点评页补真实提交、加载、失败、结果展示状态
- [x] 我的文法页接入真实创建 / 收藏混合列表和最近学习排序
- [x] 注册 / 登录页接入邀请码、待审核、已通过等真实状态分支
- [ ] 前端补全所有页面的 loading / error / retry 一致性

## P1 AI 与业务能力

- [x] 接入真实 AI 供应商
- [x] 为结构化卡片生成定义稳定输入输出 schema
- [x] 为自由造句点评定义稳定输入输出 schema
- [ ] 为 AI 输出增加结构校验与失败兜底
- 说明：已接入 OpenAI Responses API 与 Structured Outputs；当前仍缺少多次重试、降级策略和更强兜底。
- [ ] 为 AI 调用增加限流、频率控制和可观测日志
- [x] 为“游客不能创建、登录用户可创建”补真实服务端权限控制

## P1 测试与质量

- [x] 补前端 lint 配置
- [x] 补后端 lint / format 配置
- [ ] 补前端页面与 usecase 测试
- [ ] 补后端检索、创建、入库、权限边界测试
- [x] 补 API 主链路烟测
- [ ] 补至少一条端到端主链路测试
- [x] 将 lint / type-check / test 纳入统一检查脚本

## P1 Docker 与运行

- [x] 将当前 Docker 运行流程与 Alembic 迁移流程写成明确命令文档
- [ ] 解决 Windows 非 ASCII 路径下 Docker 构建上下文兼容问题的长期方案
- [ ] 区分开发、测试、生产环境配置
- [ ] 为 Docker 启动补更明确的 readiness / migration / seed 控制开关

## P2 文档与规范补齐

- [ ] 为数据库设计输出本项目版本的 `DATABASE_DESIGN` 文档
- [ ] 为前端组件结构输出本项目版本的 `FRONTEND_COMPONENTS` 文档
- [ ] 将当前开发约束拆分成更细的 `CONTRIBUTING / CONVENTIONS / ARCHITECTURE_RULES` 文档
- [ ] 明确项目正式名称与一句话目标
- [ ] 明确量化验收指标与上线门槛

## 当前建议执行顺序

1. 完成前端剩余交互细节与详情页结构化信息补齐
2. 补 AI 失败兜底、限流和可观测日志
3. 优化搜索策略与 PostgreSQL 检索能力
4. 增补前端测试、后端边界测试和至少一条端到端测试
5. 继续拆分规范文档与数据库设计文档
